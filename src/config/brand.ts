/**
 * The product name is temporary (§brief). Every user-visible occurrence reads
 * from here, so renaming is a one-line change plus the <title> in index.html.
 */
export const brand = {
  name: 'PathFinder',
  tagline: "You don't need to figure out your whole life. Just your next step.",
} as const
